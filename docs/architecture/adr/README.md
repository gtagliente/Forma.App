# docs/architecture/adr/

## Purpose

Decision records local to `exercise-service` only. For decisions that turn out to affect another service, see `../README.md` — those get promoted to `Forma.Claude`'s central `docs/architecture/adr/` instead.

## Current state

Empty. No ADRs yet.
