# docs/engineering/

## Purpose

Coding standards, git workflow, testing strategy, and devops for `exercise-service`'s own codebase.

## Current state

Empty. Tech stack and conventions aren't formally decided yet — though the inherited code under `../../src/` (from `Forma.App`) already carries real starting points worth codifying here later: `.editorconfig`, `Directory.Build.props`/`Directory.Packages.props` (central package version management), and the existing Clean-Architecture project split (`Forma.Domain`, `Forma.Application`, `Forma.Infrastructure`, `Forma.PublicApi`, `Forma.Query`). Populate this once the Service Architect has actually reviewed those choices, rather than assuming they're already the answer.
