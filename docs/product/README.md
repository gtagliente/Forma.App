# docs/product/

## Purpose

Holds the Exercise-service slice of Forma's domain model, for local reference while implementing this service.

## Authority

**Not authoritative.** `Forma.Claude`'s `docs/product/domain-model.md` and `docs/product/glossary.md` (sibling repo, `../../../Forma.Claude/docs/product/`) are the source of truth for every domain concept, including the ones this service owns. `domain-slice.md` in this folder is a derived copy of the parts relevant to `exercise-service`, kept only for convenience.

If a domain question comes up that isn't answered by the slice, or the two disagree, resolve it in `Forma.Claude` first (that's where the Analyst role operates), then resync here — don't decide domain questions locally.

## Current state

`domain-slice.md` — Exercise, Media Resource, and the Enrichment-promotion requirement, copied from `Forma.Claude` as of this service's bootstrap.
